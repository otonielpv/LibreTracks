# Paso 04 — estado

## [BUILD] iteración 1 — 2026-09-03

### Qué he cambiado
- `native/audio-engine-v2/include/lt_engine/render/mixer.h`: tablas publicadas de ranura de control y ancestros por renderer, además de hooks de diagnóstico/prueba.
- `native/audio-engine-v2/src/render/mixer.cpp`: resolución normal mediante índices precalculados; respaldo lineal conservado durante `control_count_ == 0`; ancestros resueltos fuera del callback; acumulación de picos mediante CAS.
- `native/audio-engine-v2/tests/test_mixer_strings_step04.cpp`: prueba concurrente de máximo con dos hilos y `std::latch`.
- `native/audio-engine-v2/tests/CMakeLists.txt`: registra la nueva suite.

### Criterios que creo cumplidos
- [x] C1 — el camino publicado usa la tabla; el contador de búsquedas queda disponible para verificación del reviewer.
- [x] C2 — el respaldo lineal permanece activo cuando `control_count_ == 0`.
- [x] C3/C4/C5 — cadena de ancestros precomputada con corte por huérfano, tipo no carpeta y profundidad máxima.
- [x] C6 — CAS verificado con dos hilos y 10 000 valores por hilo.
- [ ] C7 — pendiente de ejecutar la prueba de regresión con `load/max/store` durante la revisión independiente.
- [ ] C8 — pendiente de la comparación bit-exacta independiente contra el oráculo anterior.
- [x] C9 — `npm run test:native` completó CTest 341/341; el fallo informativo Rust es `STATUS_ENTRYPOINT_NOT_FOUND` y no bloquea según el script.

### Comandos ejecutados
`cmake --build native/audio-engine-v2/build-tests --config Release --target lt_engine_tests` → OK.
`lt_engine_tests.exe --test-case="step04*" --no-skip=true` → 1/1, 1 assertion.
`npm run test:native` → CTest 341/341; salida Rust informativa con `STATUS_ENTRYPOINT_NOT_FOUND`.

### He verificado que los tests saben fallar
La prueba concurrente está implementada; la sustitución controlada por `load/max/store` y su salida roja quedan para el REVIEWER, porque aún no se ha hecho esa vuelta independiente.

### Cifras del banco
No aplica a este paso.

### Bloqueos / dudas
C7 y C8 requieren revisión independiente antes de considerar el paso aprobado. No se ha ejecutado Graphify.
