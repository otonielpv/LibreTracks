## [BUILD] iteración 1 — 2026-09-03T09:39:21Z

### Qué he cambiado
- `native/audio-engine-v2/include/lt_engine/diagnostics/rt_guard.h`: guard reentrante y contadores `thread_local`, con implementación vacía y `constexpr` sin `LT_ENGINE_RT_GUARD`.
- `native/audio-engine-v2/src/render/mixer.cpp`: una sección realtime al principio de `Mixer::render`.
- `native/audio-engine-v2/CMakeLists.txt`: define privado del guard sólo para la biblioteca estática creada por el build de tests.
- `native/audio-engine-v2/tests/CMakeLists.txt`: incorpora los tests instrumentados y un target aislado que compila el contrato sin define.
- `native/audio-engine-v2/tests/test_rt_guard.cpp`: reemplazos globales de `new/delete`, exclusivos del ejecutable de tests, y pruebas directa, anidada y por hilo.
- `native/audio-engine-v2/tests/test_rt_guard_disabled.cpp`: `static_assert` del tamaño, destructor trivial y contadores nulos sin instrumentación.
- `native/audio-engine-v2/tests/test_rt_no_allocations.cpp`: sesión sintética de 4 pistas durante 100 bloques; queda desactivada explícitamente hasta el paso 03.

### Criterios que creo cumplidos
- [x] C1 — el test directo observa 0 fuera de la sección y 1 asignación/1 liberación dentro; el `operator new[]` instrumentado delega en `std::malloc`.
- [x] C2 — el test anidado conserva activa la sección exterior después de destruir la interior y detecta la asignación.
- [x] C3 — el test usa dos `std::latch`, sin esperas temporizadas; una asignación del secundario deja en cero los contadores del principal.
- [x] C4 — `rt_guard_release_contract` compila `static_assert(sizeof(...) <= 1)`, destructor trivial y ambos contadores iguales a cero sin el define.
- [x] C5 — el proyecto MSVC de producción contiene 0 apariciones de `LT_ENGINE_RT_GUARD` y `dumpbin /exports` encuentra 0 exports globales de `new/delete`; el proyecto de tests sí contiene el define.
- [x] C6 — el test existe, reproduce 100 bloques de 4 pistas e informa `allocations` y `deallocations`; se usa `doctest::skip()` hasta que el paso 03 elimine las asignaciones de routing. Ejecutado con `--no-skip`, informa 500/500 y falla como se espera.
- [x] C7 — un `new int[4]`/`delete[]` temporal dentro de `Mixer::render` elevó el fallo de 500/500 a 600/600; retirado, recompilado y suite restaurada en verde.
- [x] C8 — `npm run test:native` terminó con código 0; la suite C++ pasó completa. El fallo de enlazado Rust informativo fue `STATUS_ENTRYPOINT_NOT_FOUND`, conocido y no bloqueante según el contrato.

### Comandos ejecutados
```text
git rev-parse HEAD
147ba419a04e48e04f0695b5f93803b1e9871261

npm run test:native
100% tests passed, 0 tests failed out of 335
Total Test time (real) = 22.31 sec
Rust informativo: STATUS_ENTRYPOINT_NOT_FOUND (no gobierna el exit code)
exit 0

lt_engine_tests.exe --test-case="*realtime guard*" --no-skip=true
test cases: 3 | 3 passed | 0 failed | 333 skipped
assertions: 7 | 7 passed | 0 failed

lt_engine_tests.exe --test-case="Mixer render performs no realtime allocations" --no-skip=true
CHECK(500 == 0) FAILED
found.allocations := 500
found.deallocations := 500
exit 1 (rojo esperado hasta paso 03)

cmake --build native/audio-engine-v2/build-tests --config Release --target lt_engine_tests rt_guard_release_contract
lt_engine_tests.vcxproj -> .../Release/lt_engine_tests.exe
rt_guard_release_contract.vcxproj -> .../Release/rt_guard_release_contract.lib

ctest --test-dir native/audio-engine-v2/build-tests -C Release --output-on-failure
100% tests passed, 0 tests failed out of 335
Total Test time (real) = 20.27 sec

cmake --build native/audio-engine-v2/build-bungee-on-ffmpeg --config Release --target lt_audio_engine_v2
lt_audio_engine_v2.vcxproj -> .../Release/lt_audio_engine_v2.dll

inspección de lt_audio_engine_v2.vcxproj + dumpbin /exports
production LT_ENGINE_RT_GUARD matches: 0
test engine LT_ENGINE_RT_GUARD matches: 8
production global new/delete exports: 0
```

### He verificado que los tests saben fallar
```text
Rotura C7: añadí temporalmente new int[4]/delete[] en Mixer::render:
CHECK(600 == 0) FAILED
found.allocations := 600
found.deallocations := 600
exit 1

Restaurado: el caso explícito vuelve al rojo esperado del bug preexistente:
CHECK(500 == 0) FAILED
found.allocations := 500
found.deallocations := 500
y la suite por defecto queda verde: 335/335.

Rotura del detector: eliminé temporalmente record_allocation() de operator new[]:
test cases: 3 | 1 passed | 2 failed
CHECK(0 >= 1) FAILED en el test directo y en el anidado.

Rotura del aislamiento: eliminé temporalmente thread_local:
test cases: 1 | 0 passed | 1 failed
CHECK(5 == 0) y CHECK(6 == 0) FAILED en el test por hilo.

Todas las roturas deliberadas fueron retiradas y la versión restaurada fue
recompilada antes del ctest final.
```

### Cifras del banco
No aplica a este paso.

### Bloqueos / dudas
Ninguno.

## [REVIEW] iteración 1 — 2026-09-03T10:45:20+01:00

### Veredicto: APROBADO

### Verificación criterio a criterio
- [x] C1 — CONFIRMADO: ejecuté el caso directo con instrumentación y observé `3 passed`, 7 aserciones. Después quité temporalmente `record_allocation()` de `operator new[]`: el caso directo se puso rojo con `CHECK(0 >= 1)`; restaurado.
- [x] C2 — CONFIRMADO: el caso anidado pasa en el código entregado y, con la misma rotura temporal del contador, se puso rojo con `CHECK(0 >= 1)` después de salir de la sección interior; restaurado.
- [x] C3 — CONFIRMADO: el test usa dos `std::latch` y no contiene `sleep`. Quité temporalmente ambos `thread_local` del guard: el caso se puso rojo con `allocations == 5` y `deallocations == 6` en el hilo principal; restaurado.
- [x] C4 — CONFIRMADO: `rt_guard_release_contract` compila en Release sin el define. Cambié temporalmente el límite de tamaño a `<= 0` y la compilación falló en `test_rt_guard_disabled.cpp:5` con C2607; restaurado y recompilado correctamente.
- [x] C5 — CONFIRMADO: construí `build-bungee-on-ffmpeg` Release; su `lt_audio_engine_v2.vcxproj` tiene 0 apariciones de `LT_ENGINE_RT_GUARD`, mientras el proyecto del engine de tests tiene 8. `dumpbin /exports` sobre la DLL Release dio 0 símbolos globales `new/delete` (`??2`/`??3`).
- [x] C6 — CONFIRMADO: `test_rt_no_allocations.cpp` construye 4 pistas y ejecuta 100 bloques; captura allocations/deallocations. Está omitido por defecto y el comentario y la bitácora lo asignan explícitamente al paso 03. Con `--no-skip=true` reportó 500/500 y falló, como se documenta.
- [x] C7 — CONFIRMADO: añadí temporalmente `new int[4]`/`delete[]` dentro de `Mixer::render`; el caso explícito pasó de 500/500 a 600/600, exactamente una pareja adicional por cada uno de los 100 bloques. Retiré la sonda, recompilé y la suite restaurada quedó verde.
- [x] C8 — CONFIRMADO: ejecuté `npm run test:native`; CMake construyó el contrato sin define, CTest terminó `100% tests passed, 0 tests failed out of 335` y el comando acabó con exit 0. Los tests Rust del engine real terminaron con el `STATUS_ENTRYPOINT_NOT_FOUND` informativo que el script no considera bloqueante.

### Prueba de "sabe fallar"
- `realtime guard counts allocations only inside its section`: retiré el registro de `operator new[]` → rojo (`0 >= 1`). OK.
- `realtime guard sections are reentrant`: retiré el registro de `operator new[]` → rojo (`0 >= 1`). OK.
- `realtime guard violations are local to each thread`: retiré `thread_local` → rojo (5 allocations y 6 deallocations observadas desde el principal). OK.
- `test_rt_guard_disabled.cpp`: cambié el `static_assert` de tamaño a `<= 0` → compilación roja C2607. OK.
- `Mixer render performs no realtime allocations`: con `--no-skip` da rojo 500/500 por el bug conocido; una asignación artificial dentro de `Mixer::render` elevó el resultado a 600/600. OK.
- Todas las roturas se retiraron antes del rebuild y de las suites finales; `git diff --check` termina correctamente y el diff final conserva únicamente los cambios del BUILDER más esta REVIEW.

### Comandos ejecutados
```text
lt_engine_tests.exe --test-case="*realtime guard*" --no-skip=true
test cases: 3 | 3 passed | 0 failed; assertions: 7 | 7 passed

lt_engine_tests.exe --test-case="Mixer render performs no realtime allocations" --no-skip=true
CHECK(500 == 0) FAILED; found.allocations=500; found.deallocations=500; exit 1 esperado

rotura record_allocation() + rebuild + casos del guard
test cases: 3 | 1 passed | 2 failed; CHECK(0 >= 1) en directo y anidado

rotura thread_local + rebuild + caso por hilo
test cases: 1 | 0 passed | 1 failed; CHECK(5 == 0), CHECK(6 == 0)

rotura static_assert(sizeof(...) <= 0) + build rt_guard_release_contract
error C2607 en test_rt_guard_disabled.cpp:5

sonda new int[4] en Mixer::render + rebuild + caso no-allocations
CHECK(600 == 0) FAILED; found.allocations=600; found.deallocations=600

cmake --build native/audio-engine-v2/build-tests --config Release --target lt_engine_tests rt_guard_release_contract
ctest --test-dir native/audio-engine-v2/build-tests -C Release --output-on-failure
100% tests passed, 0 tests failed out of 335

cmake --build native/audio-engine-v2/build-bungee-on-ffmpeg --config Release --target lt_audio_engine_v2
Release/lt_audio_engine_v2.dll construido

dumpbin /exports Release/lt_audio_engine_v2.dll
global_new_delete_export_count=0

npm run test:native
C++: 335/335; exit 0 (Rust real-engine informativo: STATUS_ENTRYPOINT_NOT_FOUND)

git diff --check
exit 0
```

### Qué debe corregir el BUILDER
Nada. No quedan criterios `PENDIENTE-HUMANO`.
