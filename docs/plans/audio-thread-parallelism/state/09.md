# Paso 09 — Política de hilos, perfil de dispositivo y ajuste

## [BUILD] iteración 1 — 2026-09-03

**Entregado el lado del motor. El ajuste de UI NO está hecho.** Los criterios
que dependen de la interfaz quedan sin cumplir y así lo declaro; ver
«qué falta».

### Qué he cambiado

- `include/lt_engine/core/thread_policy.h`: `WorkerRole::Render` con su tabla, y
  `lt_resolve_render_threads()` — la función que combina recomendación,
  ajuste del usuario y `LIBRETRACKS_RENDER_THREADS` con la precedencia correcta.
- `src/ffi/engine_impl.cpp` + `include/lt_engine/engine_impl.h`: el motor
  enciende el pool al inicializarse y registra a cuántos hilos quedó, con el
  recomendado y el override al lado.
- `tests/test_render_threads_step09.cpp`: nuevo, 6 casos.

### La tabla

| núcleos lógicos | hilos de render |
| --- | ---: |
| ≥ 8 | 4 |
| 4-7 | 2 |
| ≤ 3 | 1 (pool desactivado) |

Tope en 4 con la razón escrita en el código: el banco mide **3,67x con 4 hilos y
5,46x con 8**, en una máquina ociosa de 20 hilos. La ganancia del quinto en
adelante no compensa robarle núcleos al WebView ni aumentar la probabilidad de
que un trabajador caiga en un núcleo lento y se convierta en el rezagado.

**No mira la RAM**, a diferencia de `Decode`: el render no asigna nada por
bloque (contrato del paso 02), así que la RAM no es la restricción. Hay un test
dedicado para que nadie le copie el tope por RAM a `Decode`.

El PC del usuario afectado tiene 12 núcleos (`[LT_THREADS] ... cores=12` en su
log), así que le tocan **4 hilos**.

### Criterios

- [x] **C1** — La tabla fijada valor a valor contra constantes escritas a mano,
  no contra el propio cálculo.
- [x] **C2** — `Decode`, `Fill` y `Waveform` devuelven exactamente lo mismo que
  antes. **Mi primera versión de este test falló en 4 de 12 casos**: había
  *adivinado* los valores en vez de leerlos. Ver abajo.
- [x] **C3** — `LIBRETRACKS_RENDER_THREADS` gana al ajuste y a la política.
- [x] **C4** — `0`, `-1`, `"abc"`, `""`, `999`, un número que desborda `int` y
  `" "` caen todos al recomendado. Y el propio recomendado se acota: un 500
  pasa a 8.
- [ ] **C5** — NO HECHO (necesita la UI).
- [ ] **C6** — NO HECHO (necesita la UI).
- [ ] **C7** — NO HECHO. El perfil `Constrained` de `device_profile.h` no está
  cableado: hoy la tabla sólo mira núcleos lógicos. En móvil eso da 4 hilos en
  un teléfono de 8 núcleos, que es justo lo que el criterio quería evitar.
  **Es el hueco más importante de los que dejo.**
- [x] **C8** — Demostrado que sabe fallar: ver abajo.
- [ ] **C9** — NO HECHO (necesita la UI).
- [x] **C10 (parcial)** — `npm run test:native`: **375/375**.
  `cargo check --all-targets` correcto. `npm test` completo no ejecutado.
- [ ] **C11 — PENDIENTE-HUMANO.**

### He verificado que los tests saben fallar — y C2 no sabía

La primera versión de C2 llevaba los valores de `Decode`/`Fill`/`Waveform`
escritos de memoria. Al ejecutarlo salieron **4 fallos de 12**: `Fill` da 3/4/6
donde yo puse 2/3/4, y `Waveform` se queda en 3 con 12 núcleos donde puse 4.

Los corregí con los valores **leídos de la implementación**, que es lo único que
hace útil un test de «esto no se ha movido»: si la referencia es una suposición,
el test no protege de nada — o falla siempre, o pasa por casualidad.

Que fallara es además la prueba de que el test discrimina: no compara la función
consigo misma.

### Qué falta, y por qué lo dejo declarado en vez de improvisarlo

1. **El ajuste de usuario en Ajustes → Audio** (C5, C6, C9). Es un cambio de
   pila completa: comando de Tauri, campo persistido, selector en el panel,
   textos en `es.ts`/`en.ts` y el número de hilos activos en `ResourceMeter`.
   El motor ya tiene el punto de entrada (`render_threads_user_choice_` y
   `Mixer::set_render_thread_count`), así que es cableado, no diseño.
2. **El perfil de dispositivo en la tabla** (C7). `device_profile.h` ya
   distingue `Constrained`/`Handheld`/`RoomyHandheld`; la tabla debería
   consultarlo y dar 1 en `Constrained` y 2 en `RoomyHandheld` **antes** de
   mirar núcleos. Sin eso, un móvil de 8 núcleos recibe 4 hilos de tiempo real.

Mientras tanto el pool **sí funciona en escritorio** y se puede forzar con
`LIBRETRACKS_RENDER_THREADS`, que es lo que permite validarlo con el usuario
afectado sin esperar a la UI.

### Recomendación antes de activarlo por defecto

El paso 08 dejó C5 (ThreadSanitizer) como `PENDIENTE-HUMANO`. Encender el pool
por defecto para todo el mundo sin haber pasado un detector de carreras es
precisamente el orden equivocado. Sugerencia: dejar el defecto en 1 hilo hasta
que se ejecute TSan, y validar antes con el usuario del reporte vía la variable
de entorno.

---

## [BUILD] iteración 2 — 2026-09-03

Cierra los dos huecos que dejé abiertos, uno de ellos porque el criterio estaba
mal escrito por mí.

### C7 — el perfil de dispositivo, cableado

`lt_recommend_render_threads_for(DeviceClass, cores)`, y **vive en
`device_profile.h`, no en `thread_policy.h`**. Al intentar meterla en
`thread_policy.h` salió un include circular: `device_profile.h` ya incluye
`thread_policy.h`. Eso no era un obstáculo, era la respuesta: la política de
render depende de la clase de dispositivo, así que va del lado que la conoce.

| perfil | hilos |
| --- | ---: |
| `Constrained` | 1 |
| `Handheld` | 1 |
| `RoomyHandheld` | 2 si ≥ 8 núcleos, si no 1 |
| escritorio | 4 (≥8 núcleos), 2 (4-7), 1 (≤3) |

En móvil manda el perfil y **no** el número de núcleos, con motivo: un teléfono
de 8 núcleos es big.LITTLE y con presupuesto térmico, así que decidir por el
número le daría 4 hilos de tiempo real igual que a un PC. Hay un test que fija
que ninguna clase de móvil pasa de 2 ni con 64 núcleos.

**Y quité el duplicado**: la tabla de escritorio había quedado también en
`WorkerRole::Render`. Dos copias de la misma regla es como se rompen estas
cosas en este repo (`project_region_parsers_must_mirror`), así que `Render`
desaparece del enum y queda una sola verdad.

### C5/C6/C9 — el selector se retira, y la corrección es del plan

El usuario cuestionó el criterio y tenía razón. Ninguno de los DAW de
referencia le pide a un músico un número de hilos como control principal:
Reaper y Logic lo exponen pero en automático por defecto; Ableton y Cubase sólo
dan un interruptor o un nivel. Y quien no sabe evaluarlo empeora justo en
directo.

Queda así, y el documento del paso está reescrito en consecuencia:

- Automático, decidido por la política.
- `LIBRETRACKS_RENDER_THREADS` como escotilla de soporte — resuelve el caso real
  sin publicar un mando que nadie debe tocar.
- `render_threads_active` en el snapshot, **de solo lectura**, para que un
  reporte diga a cuántos hilos corría en vez de dejarnos adivinando.

C5 y C6 quedan **RETIRADOS**, no incumplidos: ya no hay nada que cumplir.

### Comandos

```
> lt_engine_tests.exe --test-case="step09*"
[doctest] test cases:  7 |  7 passed | 0 failed
> npm run test:native            → 376/376
> cargo check --all-targets      → ok
> npm --prefix apps/desktop run test → 1054 passed (118 ficheros)
```

---

## [BUILD] iteración 3 — 2026-09-03

Corrige una contradicción mía. En la iteración 2 retiré el selector de hilos
porque «es jerga de ingeniero»… y dejé como alternativa una **variable de
entorno**, que exige abrir una terminal y lanzar la app desde ahí. Para el
usuario que originó todo esto —un músico— eso es peor que el selector.

### Qué se ha hecho

Un **interruptor** en la pestaña **Diagnóstico**, que ya existía y es de donde
salió el log que nos mandó:

> **Rendimiento del audio**
> ☐ Usar un solo hilo para el audio
> *Actívalo sólo si el sonido cruje o la aplicación va lenta. Normalmente el
> audio se reparte entre varios núcleos para ir más holgado.*
> Hilos de audio en uso: 4

Tres decisiones, y las tres tienen motivo:

- **Va en Diagnóstico, no en Ajustes → Audio.** No es un mando para afinar, es
  el «si cruje, prueba esto». En Audio invitaría a toquetearlo.
- **Es un sí/no, no un número.** Un músico no puede evaluar si le convienen 2 o
  4 hilos, y equivocarse le sale caro en directo. Hay un test que comprueba que
  no aparece ni un `combobox` ni un `spinbutton`.
- **Los hilos en uso se muestran al lado, de solo lectura.** Es lo que hace que
  el próximo reporte diga «4 hilos y sigue al 90 %».

### Cambios

- C++: comando `SetRenderThreads` (0 = automático) y su manejador, que
  recalcula con la política del dispositivo y aplica el pool entre bloques.
- Rust: `EngineCommand::SetRenderThreads`, ajuste persistido
  `audio_single_thread_render`, enviado en `apply_settings`. **Fuera del
  `rebuild_stream`** a propósito: cambiarlo no reabre el dispositivo, así que
  tocar el interruptor no corta el audio.
- TS: campo en `AppSettings`, handler, props opcionales en
  `DiagnosticsSettingsTab` (sin ellas la sección no se muestra, así los tests
  que ya existían siguen valiendo) y textos en `es.ts`/`en.ts`.

### Un detalle que costó dos intentos

El número de hilos iba dentro de la cadena traducida con `{{count}}`. El arnés
de tests **no interpola**, así que el test no podía comprobar que el número
saliera de verdad. Lo saqué fuera de la traducción: funciona igual en ambos
idiomas y ahora el test sí lo verifica.

### Criterios revisados

- [x] **C5** — Cambiar el interruptor reprograma el pool sin cortar el audio
  (el motor lo aplica entre bloques; no entra en `rebuild_stream`).
- [x] **C6** — Persiste en los ajustes y está en `es.ts` y `en.ts`.
- [x] **C9** — `render_threads_active` llega del snapshot y se muestra, de solo
  lectura.

### Comandos

```
npm run test:native            → 376/376
npm --prefix apps/desktop run test → 1057 passed (118 ficheros)
npm run lint                   → 0 errores
cargo check --all-targets      → ok
```

### Lo que esto cambia del protocolo del paso 10

Ya no hace falta pedirle al usuario que lance nada por consola. Basta con:
«abre Ajustes → Diagnóstico, copia el registro del motor y mándamelo», y si
cruje, «marca la casilla de un solo hilo y mándame otro». La cabecera
`[LT_ENGINE_INFO]` dice a cuántos hilos corría cada uno.

---

## Corrección posterior: control orientado al usuario

El control deja Diagnóstico y pasa a Ajustes > Audio como **Procesamiento
multinúcleo**, activado por defecto, siguiendo la presentación positiva de los
DAW. Desactivarlo conserva la misma escotilla de un solo hilo y el mismo campo
persistido, de modo que no invalida configuraciones existentes ni reabre el
dispositivo de audio.
