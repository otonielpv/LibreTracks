name: iOS Simulator Shots

# Runs the real app in a simulator and photographs it.
#
# Two things at once, and the second is the important one:
#
#   * App Store screenshots have to be the EXACT pixel size Apple asks for per
#     device class. A screenshot taken on a phone and sent through WhatsApp or
#     Telegram arrives rescaled (2048x946, 1280x591) and is rejected. simctl
#     writes the device's native framebuffer, so what comes out of here is
#     already the right size.
#   * The IPA declares iPad support, which means a reviewer WILL open it
#     there. This is the cheapest way to see what they will see.
#
# Split into two jobs because the two halves need DIFFERENT Xcodes: the app
# only builds with the runner's default 16.4 (26 fails in the Swift
# compatibility libraries), while idb-companion refuses to install against
# anything below a full Xcode 26. One job each, with the .app handed over as
# an artifact, beats swapping toolchains mid-job.

env:
  BUNGEE_VERSION: v2.4.24
  FFMPEG_VERSION: 7.1.1

on:
  workflow_dispatch:
    inputs:
      device:
        description: "Simulator name, exactly as `xcrun simctl list devices` prints it"
        type: string
        default: "iPad Pro 13-inch (M4)"
      settle_seconds:
        description: "Seconds to wait after launch before the first screenshot"
        type: string
        default: "25"
      language:
        description: "App language for the screenshots (App Store takes one set per language)"
        type: choice
        options: ["en", "es"]
        default: "en"

permissions:
  contents: read

concurrency:
  # Por dispositivo: un iPhone y un iPad son dos comprobaciones distintas y
  # con un grupo comun la segunda cancelaba a la primera.
  group: ios-shots-${{ github.ref }}-${{ inputs.device }}
  cancel-in-progress: true

jobs:
  build-app:
    name: Build the app for the simulator
    runs-on: macos-15
    timeout-minutes: 90

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust toolchain for the iOS simulator
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios-sim

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target-ios-sim

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Build bundled remote frontend
        run: npm run build:remote

      # Most runs of this workflow change the TOUR, not the app: the script
      # that taps through the UI is a few lines of bash, and rebuilding the
      # whole app to re-run it costs twenty minutes of runner for nothing. The
      # key covers everything that ends up inside the bundle and nothing else.
      - name: Compute the app cache key
        id: appkey
        shell: bash
        run: |
          echo "key=ios-sim-app-v1-${{ hashFiles(
              'apps/desktop/src/**',
              'apps/desktop/src-tauri/src/**',
              'apps/desktop/src-tauri/*.json',
              'apps/desktop/src-tauri/*.plist',
              'apps/desktop/src-tauri/Cargo.toml',
              'apps/desktop/src-tauri/resources/**',
              'apps/remote/src/**',
              'packages/*/src/**',
              'crates/**/src/**',
              'crates/**/build.rs',
              'native/audio-engine-v2/src/**',
              'native/audio-engine-v2/include/**',
              'native/audio-engine-v2/CMakeLists.txt',
              'Cargo.lock',
              'package-lock.json') }}" >> "$GITHUB_OUTPUT"

      # restore + save rather than the combined action, because the combined
      # one only writes the cache when the whole job succeeds. This workflow
      # failed downstream of the build often enough while it was being written
      # that throwing away a good twenty-minute build each time was the
      # difference between iterating and waiting.
      - name: Restore the built simulator app
        id: app-cache
        uses: actions/cache/restore@v4
        with:
          path: simulator-app
          key: ${{ steps.appkey.outputs.key }}

      - name: Build native audio engine and dependencies
        if: steps.app-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/ios-native-deps
        with:
          bungee-version: ${{ env.BUNGEE_VERSION }}
          ffmpeg-version: ${{ env.FFMPEG_VERSION }}
          sdk: iphonesimulator

      - name: Generate the Xcode project
        if: steps.app-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/ios-xcode-project

      # Same signing workaround as the smoke build: there is no --no-sign flag,
      # and a simulator build needs no identity.
      - name: Build the app
        if: steps.app-cache.outputs.cache-hit != 'true'
        env:
          CARGO_TARGET_DIR: ${{ github.workspace }}/target-ios-sim
        shell: bash
        run: |
          set -euo pipefail
          apple_dir="apps/desktop/src-tauri/gen/apple"
          pbxproj="$apple_dir/libretracks-desktop.xcodeproj/project.pbxproj"

          perl -0pi -e 's/CODE_SIGN_IDENTITY = "iPhone Developer";/CODE_SIGN_IDENTITY = "";\n\t\t\t\tCODE_SIGNING_ALLOWED = NO;\n\t\t\t\tCODE_SIGNING_REQUIRED = NO;/g' "$pbxproj"
          grep -q 'CODE_SIGNING_ALLOWED = NO' "$pbxproj"

          (
            cd apps/desktop
            npm run tauri -- ios build --ci --target aarch64-sim
          )

      - name: Stage the built app
        id: app
        shell: bash
        env:
          CACHE_HIT: ${{ steps.app-cache.outputs.cache-hit }}
        run: |
          set -euo pipefail
          staging="$GITHUB_WORKSPACE/simulator-app"

          if [ "$CACHE_HIT" = "true" ]; then
            app="$(find "$staging" -maxdepth 1 -type d -name '*.app' -print -quit 2>/dev/null || true)"
            if [ -z "$app" ]; then
              echo "::error::Cache hit but simulator-app/ holds no .app. Bump the key prefix to force a rebuild."
              ls -la "$staging" || true
              exit 1
            fi
            echo "Reusing the cached build: $app"
            exit 0
          fi

          apple_dir="apps/desktop/src-tauri/gen/apple"
          # Tauri moves the .app out of the xcarchive into build/<arch>/ for
          # simulator targets; fall back to the archive if that ever changes.
          app="$(find "$apple_dir/build" -maxdepth 3 -type d -name '*.app' -print -quit 2>/dev/null || true)"
          if [ -z "$app" ]; then
            app="$(find "$apple_dir" "$HOME/Library/Developer/Xcode/Archives" \
              -type d -path '*.xcarchive/Products/Applications/*.app' \
              -print -quit 2>/dev/null || true)"
          fi
          if [ -z "$app" ]; then
            echo "::error::The simulator build produced no .app"
            find "$apple_dir" -maxdepth 4 -type d -name '*.app' -print || true
            exit 1
          fi
          echo "Found $app"

          # ditto, not cp: the bundle carries symlinks and resource forks that
          # a plain copy flattens, and an app that installs but misbehaves is
          # the worst thing to cache.
          mkdir -p "$staging"
          ditto "$app" "$staging/$(basename "$app")"

          executable="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleExecutable' "$staging/$(basename "$app")/Info.plist")"
          if ! vtool -show-build "$staging/$(basename "$app")/$executable" 2>/dev/null \
            | grep -i 'platform.*simulator' >/dev/null; then
            echo "::warning::vtool does not report a simulator platform for this binary."
          fi

      # Right here, while the .app is known-good and before anything else can
      # fail.
      - name: Save the built simulator app
        if: steps.app-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: simulator-app
          key: ${{ steps.appkey.outputs.key }}

      # tar, because upload-artifact does not preserve symlinks or the
      # executable bit, and an .app that lost either installs and then fails
      # to launch.
      - name: Hand the app to the tour job
        shell: bash
        run: tar -czf simulator-app.tar.gz -C simulator-app .

      - name: Upload the app
        uses: actions/upload-artifact@v4
        with:
          name: simulator-app-${{ github.run_number }}
          path: simulator-app.tar.gz
          if-no-files-found: error
          retention-days: 1

  tour:
    name: Walk the app and take the shots
    needs: build-app
    runs-on: macos-15
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # idb-companion's formula requires a full Xcode 26; the runner defaults
      # to 16.4. This job does not compile anything, so it can simply use one.
      - name: Select an Xcode that idb accepts
        shell: bash
        run: |
          set -euo pipefail
          for candidate in /Applications/Xcode_26.0.1.app \
                           /Applications/Xcode_26.1.1.app \
                           /Applications/Xcode_26.2.app \
                           /Applications/Xcode_26.3.app; do
            if [ -d "$candidate" ]; then
              sudo xcode-select -s "$candidate"
              break
            fi
          done
          xcodebuild -version

      - name: Resolve the simulator
        id: sim
        shell: bash
        env:
          DEVICE_NAME: ${{ inputs.device }}
        run: |
          set -euo pipefail
          udid="$(xcrun simctl list devices available -j \
            | jq -r --arg name "$DEVICE_NAME" \
              '[.devices[][] | select(.name == $name)] | .[0].udid')"
          if [ -z "$udid" ] || [ "$udid" = "null" ]; then
            echo "::error::No available simulator named '$DEVICE_NAME'."
            echo "iPads on this runner:"
            xcrun simctl list devices available -j | jq -r '.devices[][].name' | grep -i ipad | sort -u
            echo "iPhones on this runner:"
            xcrun simctl list devices available -j | jq -r '.devices[][].name' | grep -i iphone | sort -u
            exit 1
          fi
          echo "Using $DEVICE_NAME ($udid)"
          echo "udid=$udid" >> "$GITHUB_OUTPUT"

      - name: Download the app
        uses: actions/download-artifact@v4
        with:
          name: simulator-app-${{ github.run_number }}

      - name: Unpack the app
        id: app
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p simulator-app
          tar -xzf simulator-app.tar.gz -C simulator-app
          app="$(find simulator-app -maxdepth 1 -type d -name '*.app' -print -quit)"
          if [ -z "$app" ]; then
            echo "::error::The artifact holds no .app"
            ls -la simulator-app || true
            exit 1
          fi
          echo "path=$GITHUB_WORKSPACE/$app" >> "$GITHUB_OUTPUT"

      - name: Boot the simulator
        shell: bash
        env:
          SIM_UDID: ${{ steps.sim.outputs.udid }}
        run: |
          set -euo pipefail
          xcrun simctl boot "$SIM_UDID"
          xcrun simctl bootstatus "$SIM_UDID" -b

          # Congelar la barra de estado. Dos motivos, y el segundo es el que
          # importa: las capturas salen con la hora canonica de Apple y la
          # bateria llena, y sobre todo el reloj deja de correr — el recorrido
          # comprueba si un toque acerto comparando el hash de la pantalla
          # antes y despues, y un cambio de minuto cambiaba el hash aunque la
          # interfaz fuera identica, dando toques fallidos por buenos.
          xcrun simctl status_bar "$SIM_UDID" override             --time "9:41"             --batteryState charged --batteryLevel 100             --wifiBars 3 --cellularBars 4 || true

      # idb drives the taps. simctl can install, launch and screenshot, but it
      # cannot touch the screen, and the tour has to dismiss two dialogs and
      # open three panels. idb reads the accessibility tree, so controls are
      # found by label instead of by coordinates.
      - name: Install idb
        shell: bash
        run: |
          set -euo pipefail
          brew tap facebook/fb
          # Homebrew refuses formulae from third-party taps until they are
          # trusted. idb is Meta's own tool and this job handles no Apple
          # credentials — it photographs an unsigned app — so the exposure is
          # a runner that is thrown away minutes later.
          brew trust --formula facebook/fb/idb-companion || brew trust facebook/fb
          brew install facebook/fb/idb-companion
          python3 -m pip install --quiet --break-system-packages fb-idb || \
            python3 -m pip install --quiet fb-idb
          idb_companion --udid "${{ steps.sim.outputs.udid }}" --only simulator &
          sleep 5
          idb connect "${{ steps.sim.outputs.udid }}" || true
          # `idb --version` does not exist. Listing targets is the real check
          # anyway: it only answers once the companion is up and talking.
          idb list-targets

      - name: Install and launch
        shell: bash
        env:
          SIM_UDID: ${{ steps.sim.outputs.udid }}
          APP_PATH: ${{ steps.app.outputs.path }}
          SETTLE: ${{ inputs.settle_seconds }}
          LANG_CODE: ${{ inputs.language }}
        run: |
          set -euo pipefail
          xcrun simctl install "$SIM_UDID" "$APP_PATH"
          # The runner's simulator is English. These launch arguments are the
          # standard way to ask for another language per launch, and the store
          # takes a separate screenshot set per language anyway.
          case "$LANG_CODE" in
            es) locale="es_ES" ;;
            *)  locale="en_US" ;;
          esac
          xcrun simctl launch "$SIM_UDID" com.libretracks.ios \
            -AppleLanguages "($LANG_CODE)" -AppleLocale "$locale"
          sleep "$SETTLE"

      - name: Walk the app and take the six shots
        shell: bash
        env:
          SIM_UDID: ${{ steps.sim.outputs.udid }}
        run: bash scripts/ios-shots-tour.sh "$SIM_UDID" "$GITHUB_WORKSPACE/simulator-shots"

      # The app's own log is the difference between "it is still loading" and
      # "it crashed on launch and the screenshot is of the home screen".
      - name: Collect the app log
        if: always()
        shell: bash
        env:
          SIM_UDID: ${{ steps.sim.outputs.udid }}
        run: |
          set -euo pipefail
          mkdir -p "$GITHUB_WORKSPACE/simulator-shots"
          xcrun simctl spawn "$SIM_UDID" log show --last 10m --style compact \
            --predicate 'processImagePath CONTAINS "LibreTracks"' \
            > "$GITHUB_WORKSPACE/simulator-shots/app-log.txt" 2>&1 || true
          tail -40 "$GITHUB_WORKSPACE/simulator-shots/app-log.txt" || true

      - name: Upload the screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LibreTracks-simulator-shots-${{ github.run_number }}
          path: simulator-shots
          if-no-files-found: error
          retention-days: 30

      - name: Shut the simulator down
        if: always()
        shell: bash
        env:
          SIM_UDID: ${{ steps.sim.outputs.udid }}
        run: xcrun simctl shutdown "${SIM_UDID:-booted}" || true
