[package]
name = "libretracks-ios-folder-picker"
version = "0.1.0"
edition = "2021"
links = "libretracks-ios-folder-picker"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = ["wry"] }
